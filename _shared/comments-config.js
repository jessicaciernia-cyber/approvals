/* Filled in by build.py from .env. Without a URL and key the notes box renders disabled
   rather than breaking the page. The anon key is public by design. */
window.APPROVAL_COMMENTS = { url: "", key: "", site: "" };
